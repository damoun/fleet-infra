---
apiVersion: netbird.io/v1
kind: NBPolicy
metadata:
  name: admins-to-kubernetes
  namespace: netbird
spec:
  name: Admins to Kubernetes
  sourceGroups:
    - admins
  destinationGroups:
    - dmn-fsn-tools
  bidirectional: false
  protocols:
    - tcp
  ports:
    - 6443
