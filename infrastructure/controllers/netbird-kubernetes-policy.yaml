---
apiVersion: netbird.io/v1
kind: NBPolicy
metadata:
  name: admins-to-kube-api
  namespace: netbird
spec:
  name: Admins to dmn-fsn-tools Kube API
  sourceGroups:
    - admins
  destinationGroups:
    - dmn-fsn-tools
  bidirectional: false
  protocols:
    - tcp
  ports:
    - 6443
