---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hcloud-csi
  namespace: flux-system
spec:
  targetNamespace: kube-system
  interval: 1h
  chart:
    spec:
      chart: hcloud-csi
      version: "2.x"
      sourceRef:
        kind: HelmRepository
        name: hcloud
        namespace: flux-system
