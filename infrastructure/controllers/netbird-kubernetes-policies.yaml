---
apiVersion: netbird.io/v1
kind: NBPolicy
metadata:
  name: admins-to-dmn-fsn-tools
spec:
  name: Admins to dmn-fsn-tools
  sourceGroups:
    - admins
  destinationGroups:
    - dmn-fsn-tools
  bidirectional: false
  protocols:
    - tcp
